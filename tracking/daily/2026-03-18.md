# 2026-03-18 - Daily Log

> **Date:** 2026-03-18
> **Sprint:** Sprint 08 - LiveKit Voice Bridge Foundation (docs-first handoff)
> **Session:** Planning / Handoff

---

## Plan for Today

- Lock the next-session voice architecture direction in markdown before any new runtime code lands
- Create a Sprint 08 handoff pack that preserves the current FastAPI duplex truth while planning the bridge architecture
- Align the main tracking docs so the next implementation session has one reading order and one first coding slice

---

## Work Notes

- [x] Reviewed the current voice runtime docs and code entrypoints before planning the new sprint
  - Confirmed the truthful active path is still `/api/v1/voice/ws/duplex`
  - Confirmed the repo does not yet contain a Node.js voice gateway, a LiveKit production path, or a Flutter/Next.js implementation slice
- [x] Created the new Sprint 08 source of truth
  - Added `tracking/sprints/sprint-08-livekit-voice-bridge-foundation.md`
  - Locked the first implementation slice around bridge bootstrap, VAD service scaffolding, and websocket fallback
- [x] Locked the architecture decision before code
  - Added `docs/decisions/ADR-015-livekit-bridge-hybrid-cutover.md`
  - Chose bridge mode over direct cutover so current websocket docs stay truthful
- [x] Added the planned bridge architecture doc
  - Added `docs/features/livekit-voice-bridge.md`
  - Documented current runtime flow, target Sprint 08 bridge flow, bootstrap contract, and VAD gRPC contract
- [x] Synced tracking pointers for the next session
  - Updated `tracking/PROJECT_STATUS.md`, `ROADMAP.md`, `tracking/tasks/backlog.md`, and `WORKFLOW_STATUS.md`
  - Moved Supabase/auth follow-up behind the new voice bridge work

### AI Collaboration Log

- Asked Codex to build a docs-first Sprint 08 handoff pack instead of rewriting runtime docs as if LiveKit already existed -> produced a new sprint file, ADR, planned bridge doc, daily log, and synced tracking updates
- Asked Codex to keep the current websocket truth intact while making the next session easier to start -> preserved current runtime docs and pointed the next handoff to the new Sprint 08 pack

---

## Issues / Bugs Encountered

| Issue | Status | Notes |
|-------|--------|-------|
| Sprint sequencing had drifted across markdown files | Resolved in planning docs | `ROADMAP.md` still pointed Sprint 07 at Supabase/auth while voice handoff docs had already moved there |
| No daily log existed yet for 2026-03-18 | Resolved | Added this file as the docs-first handoff record |
| The repo has no actual Node/LiveKit runtime slice yet | Expected | Treated as planned work, not current-state documentation |

---

## Decisions Made

- Decided to keep `/api/v1/voice/ws/duplex` as the truthful current runtime contract until bridge-mode implementation actually lands
- Decided to make Sprint 08 the next implementation-facing handoff for LiveKit voice bridge foundation
- Decided to keep current Groq plus Edge/local Indic providers for Phase 1 instead of coupling Sprint 08 to a provider migration
- Decided to keep client scope minimal by extending the existing static voice pages rather than starting Next.js or Flutter now

---

## End-of-Day Summary

**Completed:**
- Sprint 08 sprint file, ADR, planned bridge architecture doc, and synced tracking updates
- One clean read order for the next session
- Clear first coding slice for gateway bootstrap, VAD service scaffolding, and fallback wiring

**In Progress / Pending:**
- Actual implementation of `services/voice-gateway/`
- Actual implementation of `services/vad-service/`
- Creation of the first bridge-mode static client path

**Blockers:**
- No LiveKit or Node voice service currently exists in the repo
- The multilingual voice benchmark set still needs to be created before trustworthy latency comparison work

**Notes for Tomorrow:**
- Start with the Sprint 08 handoff pack, not the older generic roadmap pointers
- Keep the first code slice small: bootstrap plus health/readiness plus fallback
- Do not rewrite current runtime docs to imply LiveKit is already live

---

## Next Session Handoff

**Open these files first:**

1. `tracking/sprints/sprint-08-livekit-voice-bridge-foundation.md`
2. `docs/decisions/ADR-015-livekit-bridge-hybrid-cutover.md`
3. `docs/features/livekit-voice-bridge.md`
4. `tracking/PROJECT_STATUS.md`
5. `docs/api/websocket-voice.md`
6. `docs/features/voice-pipeline.md`
7. `src/api/websocket/voice_pkg/router.py`
8. `src/api/websocket/voice_pkg/duplex.py`
9. `src/voice/duplex_pipeline.py`

**First coding slice:**

1. Scaffold `services/voice-gateway/` with `POST /sessions/bootstrap`, `GET /health`, and `GET /ready`
2. Scaffold `services/vad-service/` with `/health`, `/ready`, and the initial gRPC/FastAPI contract surface
3. Add one small static bootstrap path that falls back to `/api/v1/voice/ws/duplex`
4. Add focused tests for bootstrap and fallback before deeper media transport work

**Verification commands to run before editing code:**

- `uv run pytest tests/unit/test_voice_agent.py -q`
- `uv run pytest tests/unit/test_pipecat_pipeline.py -q`
- `uv run pytest tests/unit/test_static_testing_lab.py -q`

---

## Files Changed Today

| Action | File | Description |
|--------|------|-------------|
| CREATE | `tracking/sprints/sprint-08-livekit-voice-bridge-foundation.md` | Added the next-session sprint source of truth for the planned voice bridge architecture |
| CREATE | `docs/decisions/ADR-015-livekit-bridge-hybrid-cutover.md` | Locked bridge mode as the next architecture decision instead of direct websocket replacement |
| CREATE | `docs/features/livekit-voice-bridge.md` | Documented the planned bridge flow, bootstrap contract, and VAD gRPC contract |
| CREATE | `tracking/daily/2026-03-18.md` | Recorded the docs-first Sprint 08 planning handoff for the next session |
| UPDATE | `tracking/PROJECT_STATUS.md` | Repointed the next-session handoff and next sprint to Sprint 08 |
| UPDATE | `ROADMAP.md` | Added the Sprint 08 sequencing addendum and pushed Supabase/auth behind the bridge work |
| UPDATE | `tracking/tasks/backlog.md` | Promoted bridge-foundation tasks into the top voice bucket |
| UPDATE | `WORKFLOW_STATUS.md` | Added the latest-session snapshot, prompt updates, and file-log entries for this handoff |

---

## Session Addendum - Sprint 09-12 Voice Program Docs

This addendum records the follow-on planning pass that expanded the voice handoff beyond Sprint 08 so the next session can stay inside the intended sprint boundary.

### What Changed

- Added dedicated sprint files for Sprint 09, Sprint 10, Sprint 11, and Sprint 12 under `tracking/sprints/`.
- Updated the main tracker docs so Sprint 08 is still the next implementation handoff, but Sprint 09-12 are now explicitly linked as the follow-on voice program.
- Updated the planned bridge docs so they now describe the full Sprint 08-12 sequence without implying LiveKit is already the active runtime.

### Added or Updated Files

| Action | File | Description |
|--------|------|-------------|
| CREATE | `tracking/sprints/sprint-09-semantic-vad-continuity-and-session-recovery.md` | Locked the semantic endpointing, continuity, and reconnect-recovery sprint boundary |
| CREATE | `tracking/sprints/sprint-10-voice-orchestration-state-and-tools.md` | Locked the orchestration, state, memory, and tool-routing sprint boundary |
| CREATE | `tracking/sprints/sprint-11-voice-load-hardening-and-observability.md` | Locked the hardening, observability, and load-testing sprint boundary |
| CREATE | `tracking/sprints/sprint-12-livekit-scale-security-and-deployment.md` | Locked the deployment, security, and cutover-readiness sprint boundary |
| UPDATE | `tracking/sprints/sprint-08-livekit-voice-bridge-foundation.md` | Added follow-on sprint references so Sprint 08 stays narrow |
| UPDATE | `docs/features/livekit-voice-bridge.md` | Added the Sprint 08-12 voice-program sequence to the planned bridge doc |
| UPDATE | `docs/decisions/ADR-015-livekit-bridge-hybrid-cutover.md` | Linked the follow-on sprint set from the accepted bridge decision |
| UPDATE | `tracking/PROJECT_STATUS.md` | Added Sprint 09-12 component rows and next-session guidance |
| UPDATE | `ROADMAP.md` | Added Sprint 09-12 to the near-term sequence and links |
| UPDATE | `tracking/tasks/backlog.md` | Split the voice backlog into Sprint 09-12 buckets |
| UPDATE | `WORKFLOW_STATUS.md` | Added the follow-on voice-program snapshot, status alignment, and file-log entries |

### Updated Next-Session Read Order

1. Read `tracking/sprints/sprint-08-livekit-voice-bridge-foundation.md`.
2. Skim `tracking/sprints/sprint-09-semantic-vad-continuity-and-session-recovery.md`, `tracking/sprints/sprint-10-voice-orchestration-state-and-tools.md`, `tracking/sprints/sprint-11-voice-load-hardening-and-observability.md`, and `tracking/sprints/sprint-12-livekit-scale-security-and-deployment.md`.
3. Read `docs/decisions/ADR-015-livekit-bridge-hybrid-cutover.md`.
4. Read `docs/features/livekit-voice-bridge.md`.
5. Cross-read `docs/api/websocket-voice.md` and `docs/features/voice-pipeline.md`.

### Validation Notes

- The new Sprint 09-12 files are linked from roadmap, status, workflow, sprint, ADR, and feature-doc surfaces.
- The current runtime docs were left untouched, so `/api/v1/voice/ws/duplex` remains the truthful live path in repo documentation.

---

## Session Addendum - Sprint 08 First Implementation Slice

This addendum records the first real Sprint 08 code landing after the docs-first handoff.

### What Shipped

- Added `services/voice-gateway/` with a strict TypeScript scaffold, health and readiness routes, `/sessions/bootstrap`, `/metrics`, and initial ring-buffer plus RMS-gate utilities.
- Added `services/vad-service/` with FastAPI probes, `/v1/vad/config`, the Sprint 08 acoustic segmenter, and the initial protobuf plus gRPC surface.
- Added local Dockerfiles plus `docker-compose.yml` entries for the new Sprint 08 services.
- Updated the premium duplex static client so it requests gateway bootstrap metadata first and visibly falls back to `/api/v1/voice/ws/duplex`.

### What Is Still Intentionally Not True

- Browser media is still using the existing duplex websocket path.
- LiveKit room token issuance now exists in the gateway scaffold, but the browser is not joining LiveKit media rooms yet.
- The gateway ring buffer and RMS gate exist as utilities, but they are not yet wired into a live downstream relay loop.

### Verification Snapshot

- `uv run pytest tests/unit/test_vad_service_segmenter.py tests/unit/test_vad_service_api.py tests/unit/test_static_testing_lab.py -q` -> passed
- `npm test` in `services/voice-gateway/` -> passed
- `npm run build` in `services/voice-gateway/` -> passed
- `docker compose config` -> passed
- `uv run pytest tests/unit/test_voice_agent.py tests/unit/test_pipecat_pipeline.py -q` -> `test_voice_agent.py` passed, `test_pipecat_pipeline.py` still has the known baseline failures around `context_intent` in stub extractors

### Files Added or Updated

| Action | File | Description |
|--------|------|-------------|
| CREATE | `services/voice-gateway/` | Added the new Sprint 08 Node gateway scaffold with bootstrap, metrics, and audio utilities |
| CREATE | `services/vad-service/` | Added the new Sprint 08 Python VAD service scaffold with FastAPI, gRPC, and segmentation logic |
| CREATE | `infra/docker/voice-gateway.Dockerfile` | Added a local dev image for the gateway |
| CREATE | `infra/docker/vad-service.Dockerfile` | Added a local dev image for the VAD service |
| UPDATE | `docker-compose.yml` | Added local dev services for `voice-gateway` and `vad-service` |
| UPDATE | `.env.example` | Added Sprint 08 service environment variables |
| UPDATE | `infra/monitoring/prometheus.yml` | Added a scrape job for the voice gateway metrics endpoint |
| UPDATE | `static/premium_voice.html` | Added gateway bootstrap configuration and a visible transport-mode pill |
| UPDATE | `static/assets/js/duplex/bootstrap.js` | Added the premium client bootstrap helper |
| UPDATE | `static/assets/js/duplex/socket.js` | Added bootstrap resolution ahead of websocket connection |
| UPDATE | `static/assets/js/duplex/app.js` | Added bootstrap-aware UI logging and transport updates |
| UPDATE | `tests/unit/test_static_testing_lab.py` | Added coverage for the premium bootstrap/fallback wiring |
| CREATE | `tests/unit/test_vad_service_segmenter.py` | Added unit coverage for the Sprint 08 acoustic segmenter |
| CREATE | `tests/unit/test_vad_service_api.py` | Added probe and config-route coverage for the VAD service |

---

## Session Addendum - Sprint 08 Relay Integration and Closeout

This addendum records the closeout slice that moved Sprint 08 from scaffold-only status into a completed bridge-foundation handoff.

### What Shipped

- Wired the gateway ring buffer and RMS gate into a real relay contract with:
  - `POST /sessions/{id}/relay/frame`
  - `POST /sessions/{id}/relay/flush`
  - `DELETE /sessions/{id}/relay`
- Added a small downstream relay service in `services/voice-gateway/` so buffered PCM can flush into the existing `/api/v1/voice/ws/duplex` runtime instead of stopping at utility-only buffering.
- Extended `services/vad-service/` with FastAPI analyze/reset endpoints so the gateway can use the Sprint 08 acoustic segmenter without blocking on a same-sprint Node gRPC client.
- Updated `static/voice_agent.html` plus new bootstrap JS so the Voice Hub now follows the same gateway-bootstrap-first, websocket-fallback truth as the premium static demo.

### Verification

- `uv run pytest tests/unit/test_vad_service_segmenter.py tests/unit/test_vad_service_api.py tests/unit/test_static_testing_lab.py -q` -> passed (`15 passed`)
- `npm run build` in `services/voice-gateway/` -> passed
- `npm test` in `services/voice-gateway/` -> passed (`26 passed`)
- Note: Vitest required an unrestricted run on Windows because sandboxed process helpers hit `spawn EPERM`; the build was already clean before that rerun.

### Files Added or Updated

| Action | File | Description |
|--------|------|-------------|
| UPDATE | `services/voice-gateway/src/audio/relay-session.ts` | Added buffered-audio read/clear support so the ring buffer participates in the downstream relay path |
| CREATE | `services/voice-gateway/src/services/vad-client.ts` | Added the gateway-side FastAPI client for Sprint 08 VAD analyze/reset calls |
| CREATE | `services/voice-gateway/src/services/downstream-relay.ts` | Added the downstream duplex websocket relay client for buffered PCM flushes |
| CREATE | `services/voice-gateway/src/services/relay-coordinator.ts` | Added the bridge contract that combines buffer state, VAD results, and downstream relay behavior |
| UPDATE | `services/voice-gateway/src/routes/relay.ts` | Added frame, flush, and reset relay routes with bridge-aware request parsing |
| UPDATE | `services/voice-gateway/src/app.ts` | Wired the relay coordinator into the gateway app |
| UPDATE | `services/voice-gateway/package.json` | Forced Vitest into a single-thread worker-safe mode for local verification |
| CREATE | `services/voice-gateway/tests/relay-coordinator.test.ts` | Added focused relay contract coverage |
| UPDATE | `services/voice-gateway/tests/relay-session.test.ts` | Updated the route assertions for the nested relay response shape |
| CREATE | `services/vad-service/app/http_models.py` | Added request/response models for the FastAPI analyze/reset surface |
| UPDATE | `services/vad-service/app/api.py` | Added `/v1/vad/analyze` and session reset endpoints |
| UPDATE | `services/vad-service/app/runtime.py` | Added session-scoped segmenter caching and reset support |
| UPDATE | `tests/unit/test_vad_service_api.py` | Added HTTP analyze/reset and invalid-base64 coverage |
| CREATE | `static/assets/js/voice-agent-bootstrap.js` | Added gateway bootstrap support for the Voice Hub static surface |
| UPDATE | `static/assets/js/voice-agent-ws.js` | Added bootstrap, reconnect token reuse, heartbeat, and fallback-aware websocket handling |
| UPDATE | `static/voice_agent.html` | Added gateway bootstrap config and a visible bridge-mode badge |
| UPDATE | `tests/unit/test_static_testing_lab.py` | Added Voice Hub bootstrap and heartbeat contract checks |
